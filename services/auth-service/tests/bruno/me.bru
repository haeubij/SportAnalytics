meta {
  name: Get My Profile
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/api/auth/me
  body: none
  auth: none
}

headers {
  x-auth-token: {{authToken}}
}

assert {
  res.status: eq 200
  res.body.email: isDefined
}
