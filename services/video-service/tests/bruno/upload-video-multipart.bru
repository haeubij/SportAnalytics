meta {
  name: Upload Video Multipart
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/videos
  body: none
  auth: none
}

headers {
  x-auth-token: {{authToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3003
}

assert {
  res.status: eq 201
  res.body.video.id: isDefined
}
