meta {
  name: Get Own Profile
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/users/me
  body: none
  auth: inherit
}

headers {
  x-auth-token: {{userToken}}
}

vars:pre-request {
  baseUrl: http://localhost:3001
}

assert {
  res.status: eq 200
}
