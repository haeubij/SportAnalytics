meta {
  name: Get All Users (Admin)
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/users
  body: none
  auth: none
}

headers {
  x-auth-token: {{adminToken}}
}

assert {
  res.status: eq 200
  res.body: isArray
}
