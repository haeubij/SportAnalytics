{
  "name": "backend-tests",
  "version": "1.0.0",
  "description": "Tests for SportAnalytics backend",
  "scripts": {
    "test": "jest --runInBand",
    "test:login": "node test-login.js",
    "test:models": "jest models/*.test.js",
    "test:routes": "jest routes/*.test.js",
    "test:all": "jest"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "mongoose": "^7.0.3",
    "express": "^4.18.2",
    "supertest": "^6.3.3"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "setupFilesAfterEnv": ["./setup.js"],
    "testTimeout": 30000,
    "verbose": true
  }
} 